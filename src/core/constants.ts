import Decimal from 'break_eternity.js';

export const zero = new Decimal(0) as const;
export const one = new Decimal(1) as const;
export const two = new Decimal(2) as const;
export const three = new Decimal(3) as const;
export const four = new Decimal(4) as const;
export const five = new Decimal(5) as const;
