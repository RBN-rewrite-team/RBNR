RAW::Proof.
 那么  也是全序的。通过归纳法，显然如果  是全序的，那么对于每个 ，\(X \cup \{A[n_0] : A \in X \land n_0 \in \mathbb{N}\} \cup \{A[n_0][n_1] : A \in X \land n_0, n_1 \in \mathbb{N}\} \cup ... \cup \{A[n_0][n_1]...[n_m] : A \in X \land n_0, n_1, ..., n_m \in \mathbb{N}\}\) 是全序的。
令 \(X_0 = \{((\underbrace{0,0,...,0,0}_n), (\underbrace{1,1,...,1,1}_n)) : n \in \mathbb{N}\}\)。由于每个 \(A \in BMS\) 对于某个 \(m \in \mathbb{N}\) 都在 \(\{A''[n_0][n_1]...[n_m] : A'' \in X_0 \land n_0, n_1, ..., n_m \in \mathbb{N}\}\) 中，显然如果 \(X_0\) 是全序的，那么对于所有 \(A, A' \in BMS\)，存在某个 \(m \in \mathbb{N}\) 使得 \(A, A' \in X_0 \cup \{A''[n_0] : A'' \in X_0 \land n_0 \in \mathbb{N}\} \cup \{A''[n_0][n_1] : A'' \in X_0 \land n_0, n_1 \in \mathbb{N}\} \cup ... \cup \{A''[n_0][n_1]...[n_m] : A'' \in X_0 \land n_0, n_1, ..., n_m \in \mathbb{N}\}\)，这是全序的，因此 \(A, A'\) 是可比较的。所以如果 \(X_0\) 是全序的，那么 \(BMS\) 是全序的。
现在只需证明 \(X_0\) 是全序的。这是容易的，因为 \(((\underbrace{0,0,...,0,0}_{n+1}), (\underbrace{1,1,...,1,1}_{n+1}))[1]\) 显然是 \(((\underbrace{0,0,...,0,0}_n), (\underbrace{1,1,...,1,1}_n))\)，因此通过归纳法，对于每个 \(n < m \in \mathbb{N}\)，有 \(((\underbrace{0,0,...,0,0}_n), (\underbrace{1,1,...,1,1}_n)) = ((\underbrace{0,0,...,0,0}_m), (\underbrace{1,1,...,1,1}_m))\underbrace{[1][1]...[1][1]}_{m-n} < ((\underbrace{0,0,...,0,0}_m), (\underbrace{1,1,...,1,1}_m))\)，并且 \(X_0\) 的所有元素都具有这种形式，所以 \(X_0\) 的所有元素都是两两可比较的。
推论 2. \(BMS\) 的序与字典序（列按字典序比较）一致。
For every non-empty \(A \in BMS\),\(A[0]\) is simply \(A\) without the last column,
 as it is equal to \(G + B_0\), and thus \(A = A[0] + (C)\). Then it is trivial
 to prove by induction that for all \(A, A' \in BMS\) , if \(A'\) is a subsequence of
 \(A\), then \(A' = A\underbrace{[0][0]...[0][0]}_n\)
 for some \(n \in \mathbb{N}\), and thus \(A' \le A\). Together
 with \(A[n]\) being a subsequence of \(A[n+1]\) forall \(A \in BMS\) and \(n \in \mathbb{N}\), this
 also means that for all \(A, A' \in BMS\) and \(n \in \mathbb{N}\) , \(A[n] \le A[n+1]\), and if
\(A[n] < A' \le A[n+1]\), then \(A[n] \le A'[0]\). This implies that if some subset \(X\) of\(BMS\) is totally ordered, 
then \(X \cup \{A[n] : A \in X \land n \in \mathbb{N}\}\) is
 also totally ordered. By induction, it is clear that if \(X \subseteq BMS\) is totally
 ordered, thenX∪{A[n0] :A∈X∧n0∈N}∪{A[n0][n1] :A∈X∧n0,n1∈
 N}∪...∪{A[n0][n1]...[nm] :A∈X∧n0,n1,...,nm∈N}istotallyorderedforeach
 \(m \in \mathbb{N}\).LetX0={((0,0,...,0,0 | {z }
 n
 ),(1,1,...,1,1 | {z }
 n
 )):n∈N}. SinceeachA∈BMS
 is in{A′′[n0][n1]...[nm] :A′′∈X0∧n0,n1,...,nm∈N}forsomem∈N, it is
 obviousthat ifX0 istotallyordered, thenforallA,A′∈BMS, there’ssome
 m∈NsuchthatA,A′ ∈X0∪{A′′[n0] :A′′ ∈X0∧n0∈N}∪{A′′[n0][n1] :
 A′′∈X0∧n0,n1∈N}∪...∪{A′′[n0][n1]...[nm] :A′′∈X0∧n0,n1,...,nm∈N},
 whichistotallyordered,andthereforeA,A′ arecomparable.SoifX0 istotally
 ordered,thenBMSistotallyordered.
 It isnowsufficienttoprovethatX0 istotallyordered. This iseasy, since
 ((0,0,...,0,0 | {z }
 n+1
 ),(1,1,...,1,1 | {z }
 n+1
 ))[1] istrivially((0,0,...,0,0 | {z }
 n
 ),(1,1,...,1,1 | {z }
 n
 )),andthus
 byinduction, foreachn<m∈N,((0,0,...,0,0 | {z }
 n
 ),(1,1,...,1,1 | {z }
 n
 ))=((0,0,...,0,0 | {z }
 m
 ),
 (1,1,...,1,1 | {z }
 m
 ))[1][1]...[1][1] | {z }
 m−n
 <((0,0,...,0,0 | {z }
 m
 ),(1,1,...,1,1 | {z }
 m
 )),andallelementsofX0
 areofthisform,soallelementsofX0arepairwisecomparable.
 Corollary2.4.TheorderingofBMScoincideswiththelexicographicalorder
ingwithcolumnscomparedlexicographically.
