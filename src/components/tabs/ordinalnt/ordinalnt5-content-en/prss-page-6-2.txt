RAW:: Use structural induction based on the construction from \(P\). If \(S = E\), the case is trivial. If there exists \((T,U) \in P^2\) such that \(S = \textrm{gen}(T,U)\), and both satisfy the condition. If \(U = E\), then by Lemma 2.1, \(\textrm{expand}(S,n) = T \in P\). Next, assume there exists \((V,W) \in P^2\) such that \(U = \textrm{gen}(V,W)\), and both satisfy the condition. If \(W = E\), then \(S = \textrm{gen}(T,\textrm{gen}(V,E))\), and by Lemma 2.2, \(\textrm{expand}(S,n) = T \frown \underbrace{(0) \frown V^+ \frown \cdots \frown (0) \frown V^+}_n\). Here, using mathematical induction on \(n\), we have \(\textrm{expand}(S,0) = T \in P\), and \(\textrm{expand}(S,k+1) = \textrm{expand}(S,k) \frown (0) \frown V^+ = \textrm{gen} (\textrm{expand}(S,k),V)\), thus it can be shown that for any \(n \in \mathbb N\), \(\textrm{expand}(S,n) \in P\). If \(W \neq E\), then \(S = \textrm{gen}(T,\textrm{gen}(V,W))\), and by Lemma 2.3, \(\textrm{expand}(S,n) = \textrm{gen}(T,\textrm{expand}(\textrm{gen}(V,W),n)) = \textrm{gen}(T,\textrm{expand}(U,n)) \in P\). Therefore, for any \(S \in P\), either \(S = E\) or \(\textrm{expand}(S,n) \in P\).
Lemma 2 is a method of classifying the behavior of \(\textrm{expand}\) via the structure of \(P\). Lemma 3 shows that the output of \(\textrm{expand}\) is an element of \(P\).
Recursively define a map \(\textrm{trans} : P \to \varepsilon_0\) via the structure of \(P\). Note that \(\varepsilon_0\) is an ordinal closed under addition and \(\omega\)-power. - If \(S = E\), then \(\textrm{trans}(S) := 0\). - If there exists \((A,B) \in P^2\) such that \(S = \textrm{gen}(A,B)\), then \(\textrm{trans}(S) := \textrm{trans}(A) + \omega^{\textrm{trans}(B)}\).